#include "main.h"

/**
*_puts_recursion - print string
*
*@s: string
*@i: compteur
*
*return: 0
*/

void _puts_recursion(char *s)

{
	int i = 0;

	if (s[i] != '\0')
	{
		return (0);
	}
	i++;
	_putchar(s[i]);
}

/**
 * main - return 0 or puts
 * 
 * return: 0
 */

int main ()

{
	if (s[i] == '\0')
	{
		return (0);
	}
	else
	return (_puts_recursion);
}
